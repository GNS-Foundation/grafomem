# GRAFOMEM benchmark corpus manifest (R4 seed registry).
#
# Human-authored. Lists the dimensions of the published suite; the builder
# (scripts/generate_corpus.py) expands the workload x seed x difficulty
# cross-product, generates + validates each trace, and records the R3
# corpus content hash (and per-workload rollup hashes) in corpus.lock.

name = "grafomem-bench-v1.0.0"
schema_version = "1.0.0"  # baseline; W10 traces self-declare "0.2.0"
generator_version = "1.0.0"
description = "GRAFOMEM v1.0.0 suite. All workloads W1-W10 locked, including W8 (Forgetting Curve / Summarise-Merge). Two-sided conformance. R3 byte-identical reproducible. ANN excluded from explicit conformance generation. Phantom anomaly probe deferred. 5 seeds x 3 difficulties."

workloads = ["W1", "W2", "W3", "W4", "W5", "W6", "W7", "W8", "W9", "W10"]
seeds = [0, 1, 2, 3, 4]
difficulties = ["easy", "medium", "hard"]
