# GRAFOMEM benchmark corpus manifest (R4 seed registry).
#
# Human-authored. Lists the dimensions of the published suite; the builder
# (scripts/generate_corpus.py) expands the workload x seed x difficulty
# cross-product, generates + validates each trace, and records the R3
# corpus content hash (and per-workload rollup hashes) in corpus.lock.

name = "grafomem-bench-v0.1.8"
schema_version = "0.1.3"
generator_version = "0.1.0"
description = "GRAFOMEM v0.1.x suite. W1 (Stable Recall) + W2 (Drift & Conflict) + W3 (Distractor Noise) + W4 (Long-Horizon Dependencies) + W5 (Tenant Isolation) + W6 (Deletion / Leakage) + W7 (Conflict Detection) + W9 (Cross-Session Deletion). W8 (Forgetting Curve) is built but intentionally held out of the lock. 5 seeds x 3 difficulties."

workloads = ["W1", "W2", "W3", "W4", "W5", "W6", "W7", "W9"]
seeds = [0, 1, 2, 3, 4]
difficulties = ["easy", "medium", "hard"]
